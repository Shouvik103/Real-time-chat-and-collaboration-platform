const { io } = require('socket.io-client');

async function test() {
    // connect a socket without auth, wait it will fail.
    // Let me just test the backend emitter if I could.
    console.log("Just running a test check");
}
test();
